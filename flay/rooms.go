package barf

//rooms has room based funcs

//GenPlan generates a floor plan
func (f *Flr) GenPlan(rmap map[int]*Rm, nmap map[Pt][]*Wall, ptmap map[Pt][]int, wmap map[Tupil][]int, pts []Pt)(err error){
	return
}
